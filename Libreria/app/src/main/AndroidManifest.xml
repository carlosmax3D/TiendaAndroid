<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.squareraresoftware.libreria" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_t"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Tienda"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".agregar"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".buscar"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".listar"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".modificar"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".pedido"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".listarProveedores"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".agregarProveedores"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".modificarProveedores"
            android:label="@string/app_name" >
        </activity>
    </application>

</manifest>
